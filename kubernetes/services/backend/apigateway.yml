# service for spring boot microservice adverseaction
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
spec:
  ports:
    - port: 4000
  selector:
    app: api-gateway
    project: bootcamp99